import { ErrorMessageForDirective } from './error-message-for.directive';

describe('ErrorMessageForDirective', () => {
	it('should create an instance', () => {
		const directive = new ErrorMessageForDirective();
		expect(directive).toBeTruthy();
	});
});
